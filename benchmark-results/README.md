# Benchmark Results

These files are generated by the real benchmark harness in `orchestrator/src/integrationTest/java/com/helios/orchestrator/integration/RealBenchmarkRunner.java`.

The current checked-in run used real local infrastructure through Docker Compose:

- DynamoDB Local on the Docker network.
- Kafka 3.7.0 on the Docker network.
- Redis 7.4 on the Docker network.

## Files

| File | Source |
| --- | --- |
| `benchmark-summary.json` | Consolidated benchmark output |
| `scheduling-latency-jmh.json` | Trigger-to-first-step latency generated by the Java real-infra harness |
| `throughput-gatling/real-throughput.json` | Sustained workflow trigger throughput generated by the Java real-infra harness |
| `chaos-test-results.txt` | Crash-window replay/idempotency result |
| `crash-recovery-times.csv` | 100 recovery timing trials |
| `ai-synthesis-accuracy.json` | Parser-backed synthesis validation set; not a real LLM benchmark |

## Current Claim

The current run supports these claims:

- Real integration tests passed against DynamoDB Local, Kafka, and Redis.
- 1,000 crash-window replay injections produced 0 duplicate step attempts.
- 100 recovery trials of 100 in-flight workflows completed below the 5 second target in this local run.
- A 20 second calibration run at 50 workflows/sec completed all 1,000 workflows with 0 failures.

It does not support a 500 workflows/sec claim yet. Run `./scripts/run-real-benchmark.sh` on stronger hardware to produce a full target run.
